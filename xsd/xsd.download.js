const
    util     = require('../ontology.util.js'),
    prefix   = 'xsd',
    ontology = 'http://www.w3.org/2001/XMLSchema#',
    override = false;

Promise.all([]).then(util.logDone).catch(util.logError);
